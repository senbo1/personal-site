export type Project = {
  title: string;
  description: string;
  demoUrl: string;
  githubUrl: string;
};

export type Blog = {
  title: string;
  link: string;
  date: string;
};

export type Link = {
  title: string;
  link: string;
};

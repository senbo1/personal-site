export type Project = {
  title: string;
  description: string;
  link: string;
};

export type Blog = {
  title: string;
  link: string;
  date: string;
};

export type Link = {
  title: string;
  link: string;
};

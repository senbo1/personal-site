export type CardItem = {
  title: string;
  description: string;
  url: string;
};

export type Blog = {
  title: string;
  link: string;
  date: string;
};

export type Link = {
  title: string;
  link: string;
  icon: string;
};
